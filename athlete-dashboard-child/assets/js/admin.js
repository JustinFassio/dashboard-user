jQuery(document).ready(function($) {
    // Initialize cache stats widget if it exists
    if ($('#cachePerformanceChart').length) {
        console.log('Cache Performance Chart initialized');
    }
}); 